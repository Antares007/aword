#include "aradani.h"
#include "aword.h"
#include <stdio.h>
void print(aword) {
  if (r == 1 && d == -3)
    printf("%s", (char *)o[w + 1]);
  m(a, w, o, r, d);
}
#include <assert.h>
#define Tp(text) Ta(print, text)
void stop(aword) {
  if (r == 3 && d == 3)
    m(a, w, o, 1, -3);
  else
    m(a, w, o, r, d);
}
S(toti, {
  T(b) Tp("Blue") LastArm;
  T(b) Tp("DarkBlue") RightArm;
});
int main() {
  long a = 512;
  void *o[a];
  long r = 3, d = 3;
  T(b)
  Ta(print, " ")
  Ta(print, "White")
  Ta(print, " ")
  T(toti)
  Ta(print, " ")
  T(toti)
  Ta(print, " ")
  Ta(print, "\n")
  T(dot) m(a, a + 1, o, r, d);
  return 0;
}
